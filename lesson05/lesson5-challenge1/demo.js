function squaredNumber(firstNumber) {
    let input =  prompt ('Enter a number');
    let number= input;
    let squaredNumber = number * number;

    let result = squaredNumber;

    alert(result);
}
